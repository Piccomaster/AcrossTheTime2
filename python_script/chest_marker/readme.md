After running the script, simply input a path like D:\py_script\att2_optimize\chest_pos\Coffres.xlsx,
then provide the path to the quest_item_id.xlsx file in the script folder. 
This will automatically generate a summon_marker file, which can then be replaced.