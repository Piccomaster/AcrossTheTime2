Run the script, enter the file path of "macro loot table (new).xlsm" in Google Sheets, and execute it. 
After that, two folders will be output: one is the "entities" folder, and the other is the "chest" folder, which contain the corresponding loot tables respectively. 
You just need to copy these two folders into the "loottable" folder and replace the original ones.